from pathlib import Path

HL_DIR = Path.home() / ".hl"
DB_PATH = HL_DIR / "hl.db"
ORDER_CONFIG_PATH = HL_DIR / "order-config.json"
SERVER_PID_PATH = HL_DIR / "server.pid"
SERVER_LOG_PATH = HL_DIR / "server.log"
SERVER_STATE_PATH = HL_DIR / "server-state.json"
SERVER_CACHE_PATH = HL_DIR / "server-cache.json"
